{
  "name": "rental-real-app",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js && node --check service-worker.js && node --check rental-math.js && node --check email-notifications.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "mongodb": "^6.20.0",
    "nodemailer": "10.0.8"
  },
  "engines": {
    "node": ">=20"
  }
}
